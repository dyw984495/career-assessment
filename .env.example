## 环境变量模板 - 请复制为 .env.local 并填入实际值

# ========== Supabase（必需）==========
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ========== 应用 URL（用于生成二维码链接）==========
# 本地开发：可留空或注释掉；构建时会自动用当前浏览器 origin，避免端口与 Vite 不一致导致复制链接白屏。
# VITE_APP_URL=http://localhost:5173
# 生产环境（Cloudflare Pages）必填：
# VITE_APP_URL=https://your-app.pages.dev

## ========== Supabase Edge Functions（服务端，仅部署时在 Supabase 控制台设置）==========
# 以下变量不需要在 .env.local 中配置，它们在 Supabase Edge Functions 环境中自动可用：
# - SUPABASE_URL
# - SUPABASE_SERVICE_ROLE_KEY
# - SUPABASE_ANON_KEY
